<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"> 
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
  </head>


<body>

    <%# <% if flash[:messages] %>
    <%# <h2>Errors</h2>
    <ul>
    <% flash[:messages].each do |error| %>
    <%# <li><%= error %>
    <%# <% end %> 
    <%# </ul>
    <% end %> 

<%# There are bugs with this that still need to be worked on. 
right now if you try to make a shelf it gives an error saying that the user must exist
with the error messages commented out, the form doesn't actually create the shelf.%>
<%# Either way a shelf isnt being created. %>
    <p><%= form_for @shelf do |f|%></p>
    <p><%= f.label :name %></p>
    <p><%= f.text_field :name %></p>
    <p><%= f.label :user %></p>
    <p><%= f.collection_select :user_id, User.all, :id, :username %></p>

    <%= f.submit%>
    <% end %>



</body>
</html>